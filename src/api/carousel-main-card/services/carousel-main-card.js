'use strict';

/**
 * carousel-main-card service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::carousel-main-card.carousel-main-card');
